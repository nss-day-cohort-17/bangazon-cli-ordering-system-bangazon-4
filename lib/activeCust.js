'use strict';

const db = require("sqlite3").verbose();

module.exports.getCustomer = () => {
  let customers = []
  return customers;
}

module.exports.getBuyer = (id) => {
  let buyer = {id}
  return buyer;
}
